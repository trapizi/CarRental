CREATE TABLE CONSULTATION
( 
CONSULTATION_NUM INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 100, INCREMENT BY 1),
CONSULTATION_PRICE FLOAT,
CONSULTATION_TIME time,
CONSULTATION_DATE date,
CORPORATE_ID INTEGER NOT NULL CONSTRAINT CORPORATE_ID_FK1
		REFERENCES CORPORATE ON DELETE CASCADE ON UPDATE RESTRICT
)