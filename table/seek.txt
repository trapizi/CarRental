create table SEEK
(
	SEEK_ID INTEGER PRIMARY KEY NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
	MEMBER_ID INTEGER NOT NULL CONSTRAINT MEMBER_ID_FK1
	REFERENCES MEMBER ON DELETE CASCADE ON UPDATE RESTRICT,
	BOOK_DAY TIMESTAMP NOT NULL,
	BOOK_TIME TIMESTAMP NOT NULL
)
