create table SEEK
(
	SEEK_ID INTEGER PRIMARY KEY NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
	USERNAME LONG VARCHAR NOT NULL,
	BOOK_DAY DATE NOT NULL,
	BOOK_TIME TIMESTAMP NOT NULL
)
