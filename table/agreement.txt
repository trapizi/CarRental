create table AGREEMENT
(
	AGREEMENT_ID INTEGER PRIMARY KEY NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
	SEEKER INTEGER,
	OFFERER INTEGER,
	PAYAMT FLOAT,
	AGREEDATE DATE,
	CREATEDAY DATE,
	TOPOSTCODE BIGINT,
	FROMPOSTCODE BIGINT
)