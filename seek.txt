create table SEEK
(
	SEEK_ID INTEGER PRIMARY KEY NOT NULL GENERATE ALWAYS AS IDENTIY (START WITH 1, INCREMENT BY 1),
	USERNAME VARCHAR NOT NULL,
	BOOK_DAY DATETIME NOT NULL,
	BOOK_TIME TIMESTAMP NOT NULL
)
