create table OFFER
(
	CAR_ID INTEGER PRIMAY KEY NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
	SEATS INTEGER,
	CAR_TYPE VARCHAR,
	BRAND VARCHAR,
	TRANSMISSION VARCHAR
	FUEL_TYPE VARCHAR,
	PRICE FLOAT(12,2)
)